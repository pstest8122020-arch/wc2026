# Live config of the production app (matches `fly config show -a wc2026-zfovlq`)
app = 'wc2026-zfovlq'
primary_region = 'iad'

[build]
  dockerfile = 'Dockerfile'

[env]
  DB_PATH = '/data/wc2026.db'
  FOOTBALL_COMPETITION_CODE = 'WC'
  NODE_ENV = 'production'
  PORT = '8080'
  SOLANA_RPC_URL = 'https://api.mainnet-beta.solana.com'

[[mounts]]
  source = 'wc26data'
  destination = '/data'
  initial_size = '1gb'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

  [http_service.concurrency]
    type = 'requests'
    hard_limit = 250
    soft_limit = 200

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '30s'
    method = 'GET'
    path = '/api/health'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
  cpus = 1
