{
  "name": "discord-alpha-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Historic call-volume analytics for Discord alpha channels (Jupiter)",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "ingest": "npm run ingest --workspace=server",
    "build": "npm run build --workspace=client",
    "start": "NODE_ENV=production npm start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
